The button to download or copy json doesn't seem to work, please check it. Clicking on a highscore should automatically load the replay. Bears shouldn't move immediately after they are placed, they should only move after the next turn (maybe make it configurable, along with an option to not spawn rocks and another to not spawn bears and another to not spawn crystals. These settings will also need to be part of the replay.) please create some simple but pretty svgs to represent each tile type. Animate the bear movement, to make it easier to visualise when it moves. If a player tries to place a tile while the bear is moving, immediately move the bear before deciding whether the clicked on tile is free or not. Add another setting to control animation duration - from 0 to 200 ms. Default to 100ms. Save in local storage, but animation speed doesn't need to be in the replay. When starting a new game, randomly place up to 5 tiles.

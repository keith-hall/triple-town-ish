Clicking on a highscore should automatically load the replay (maybe it already does, I didn't check...) Bears shouldn't move immediately after they are placed, they should only move after the next turn (maybe make it configurable, along with an option to not spawn rocks and another to not spawn bears and another to not spawn crystals. These settings will also need to be part of the replay. Also the replay moves should state which tile was placed even if it is technically redundant info because it can be gleaned from the rng seed, in case tiles are expanded in future etc.) please create some simple but pretty svgs to represent each tile type. Animate the bear movement, to make it easier to visualise when it moves. If a player tries to place a tile while the bear is moving, immediately move the bear before deciding whether the clicked on tile is free or not. Add another setting to control animation duration - from 0 to 200 ms. Default to 100ms. Save in local storage, but animation speed doesn't need to be in the replay. When starting a new game, randomly place up to 5 tiles (such that they wouldn't merge). (Also make configurable.)
Please also include instructions how to build and run the project locally, assuming the repository is already cloned. I think crystals should turn into rocks when placed and not merged. Merging more than 3 tiles should give a small boost or multiplier to the score for merging, based on the number of additional tiles over 3.

Sometimes the bear movement seems a bit weird, like it animates from the wrong place or from multiple tiles or something. Please investigate and fix. Let's move the settings pane in responsive (mobile) mode to be below the score, otherwise it's hard to see the next piece without scrolling, breaking immersion. Let's animate merges similar to how we do with bear movement, so it is more visual and apparent what happened. So let's say placing a grass tile, you see it merge into a bush, that bush is next to two other bushes, so then we would see the animation of those tiles all moving into the tile which becomes a tree. So the bear animation speed setting could become just the animation speed. On devices with a mouse, when the mouse is over a free tile, let's show a preview of the tile which the user can place. The preview should be semi-transparent so we can see it is just a preview. Also, if it would cause merges, let's pulse or wobble the tiles it would merge (including the preview), for a clear visual effect. So that rocks don't constantly just get in the way, how about we add an optional mechanic to crack the rock when a merge occurs adjacent to it. (i.e. merging into a tile touching a rock tile. Merging away should have no effect.) Show it visually by drawing a crack on the rock, one extra crack for each merge which occurred nearby. After 5 such merges, the rock is split and removed from the grid, freeing the space up.
